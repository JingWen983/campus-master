# 编译产物
*.o
*.obj
*.pdb
*.exe
*.dll
*.lib
*.exp
*.bat

# 构建工具临时文件
.vs/
*.user
*.suo
*.sdf
*.opensdf
*.aps
*.opendb
*.VC.db
*.VC.opendb

# 测试脚本与产物（不入库，仓库仅保留源代码）
test_*.py
test_*.js
test_*.ts
verify_*.py
check_*.py
*_test.py
*_test.ts
conftest.py

# tests/ 与 docs/ 整体不入库（按维护者要求：仓库不保留这两个目录）。
# 变更说明（此前 tests/*.cpp 与 tests/run_tests.ps1 是被刻意放行的）：
#   * 这两个目录现在**完全**被忽略，与「仓库不保留 tests/ 与 docs/」的口径一致；
#   * 磁盘上的文件**保留**（本地仍可跑 tests/run_tests.ps1、仍可本地看文档），
#     只是不再进入版本控制；
#   * 由此产生的连带影响（已在同批提交里同步修正）：
#       - CI / release 的打包步骤不再 `cp -r docs/.`（checkout 里已无 docs/），
#         交付包因此**不再附带 docs/ 用户文档**；docs/audit 的「排除+反向守卫」
#         也随之失去意义，一并移除；
#       - README 中指向 docs/ 与 tests/ 的链接会失效，已在该文件中同步移除。
tests/
docs/

test/
test_*.csv
test_*.png
test_*.exe
screenshots/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/

# 冗余压缩包与重复源码
*.zip
sqlite_src/

# 本地依赖工具目录（当前构建未使用，保留在本地、不入库）
vcpkg/
.zcode/

# 安全修复 V16：禁止提交 Cookie / 凭据文件
cookies*.txt
*.cookie
*.session
.env
*.pem
*.key

# 系统文件
Thumbs.db
Desktop.ini
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# 安全修复 V16：日志和所有运行时数据库一律不入库
# 种子数据如需保留请改名为 seed.db 并加入 docs/seed/ 目录单独管理
*.log
*.db
*.db-journal
*.db-shm
*.db-wal

# Trae 工具目录（spec 文档可选择性保留，这里排除）
.trae/

# AgentTeams 运行时状态（团队 / 任务 / 信箱等本机状态，不入库）
# 该目录是本机调度工具的运行时状态（当前约 860 KB，含 captain 与各 member 的 jsonl 信箱），
# 与项目源码无关。此前它只是"未跟踪"（git status 显示 ??），容易被误 add，
# 故显式忽略；仓库只保留源码与文档。
.agent-teams/

# 前端工程（Vite + Vue SFC）
frontend/node_modules/
frontend/dist/
frontend/.vite/
