# 编译产物
*.o
*.obj
*.pdb
*.exe
*.dll
*.lib
*.exp
*.bat

# 构建工具临时文件
.vs/
*.user
*.suo
*.sdf
*.opensdf
*.aps
*.opendb
*.VC.db
*.VC.opendb

# 测试脚本与产物（不入库，仓库仅保留源代码）
test_*.py
test_*.js
test_*.ts
verify_*.py
check_*.py
*_test.py
*_test.ts
conftest.py

# tests/ 默认整体排除（编译产物、临时数据、截图一律不入库），但按文件放行
# 单元测试的**源码**与驱动脚本：
#   * 放行的只有 tests/*.cpp 与 tests/run_tests.ps1，两者都是源代码 ——
#     不违反「仓库仅保留源代码」的既有意图；编译产物（tests/pbkdf2_test.exe）
#     仍同时被上面的 *.exe 与本条 tests/* 命中，不会被跟踪；
#   * 必须写成 `tests/*` + 逐条 `!` 否定，不能写成裸 `tests/`：git 规则规定
#     「父目录被排除时无法再放行其内部文件」，裸 `tests/` 会让下面的 ! 失效；
#   * 若将来新增其他类型的测试源码，需在此追加对应的 `!tests/*.<ext>`。
tests/*
!tests/*.cpp
!tests/run_tests.ps1

test/
test_*.csv
test_*.png
test_*.exe
screenshots/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/

# 冗余压缩包与重复源码
*.zip
sqlite_src/

# 本地依赖工具目录（当前构建未使用，保留在本地、不入库）
vcpkg/
.zcode/

# 安全修复 V16：禁止提交 Cookie / 凭据文件
cookies*.txt
*.cookie
*.session
.env
*.pem
*.key

# 系统文件
Thumbs.db
Desktop.ini
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# 安全修复 V16：日志和所有运行时数据库一律不入库
# 种子数据如需保留请改名为 seed.db 并加入 docs/seed/ 目录单独管理
*.log
*.db
*.db-journal
*.db-shm
*.db-wal

# Trae 工具目录（spec 文档可选择性保留，这里排除）
.trae/

# AgentTeams 运行时状态（团队 / 任务 / 信箱等本机状态，不入库）
# 该目录是本机调度工具的运行时状态（当前约 860 KB，含 captain 与各 member 的 jsonl 信箱），
# 与项目源码无关。此前它只是"未跟踪"（git status 显示 ??），容易被误 add，
# 故显式忽略；仓库只保留源码与文档。
.agent-teams/

# 前端工程（Vite + Vue SFC）
frontend/node_modules/
frontend/dist/
frontend/.vite/
